package com.propper.page.validators.mobile;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.propper.page.validators.web.LoginValidator;

/**
 * This class contains all validations related to Login Page when opened in
 * mobile browser (chrome at this stage).
 * 
 * @author Jaikant
 *
 */
public class MLoginValidator extends LoginValidator {

	private static final Logger LOGGER = LoggerFactory.getLogger(MLoginValidator.class);

	// override parent class method here if new implementation needed
	// you can also add new validation methods here if needed

}
